#!/usr/bin/env bash
# Yoke launcher: prefer the repo venv, fall back to system python3.
set -euo pipefail
HERE="$(cd "$(dirname "$0")" && pwd)"
cd "$HERE"
if [ -x "$HERE/.venv/bin/python" ]; then
  exec "$HERE/.venv/bin/python" -m src.yoke "$@"
else
  exec python3 -m src.yoke "$@"
fi
